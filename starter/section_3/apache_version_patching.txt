#Provide the apache version and configuration lines to be changed 

Endpoint:
192.168.72.131
192.168.137.133

# Apache Version
ubuntu@ubuntu-VirtualBox:~$ apache2 -v
Server version: Apache/2.4.7 (Ubuntu)
Server built:   Apr  3 2019 18:04:59


# Configuration file change(Hint: the change has to be made in /etc/apache2/conf-enabled/security.conf)
Edit your Apache configuration file/etc/apache2/httpd.conf and add the following

ServerTokens Prod
ServerSignature Off
TheServerTokens will change Header to only display the web server type

The ServerSignature directive will remove the version information from the page generated by Apache.

Reload Apache
Redhat:
    systemctl reload httpd.service

Ubuntu:
    sudo service apache2 restart
Check your website’s HTTP Response Header again. Now it should only show

Server: Apache

Resource: https://nowherelan.com/2018/12/27/remove-server-version-banner-in-apache/

